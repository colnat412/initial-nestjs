import { Injectable } from "@nestjs/common";

@Injectable()
export class JWTStrategy {
  // This class would typically extend Passport's JWT strategy
  // and implement methods to validate JWT tokens.
  // For the sake of this example, we will leave it empty.
}
